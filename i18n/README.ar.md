[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# SyncImagingSystem


![Python](https://img.shields.io/badge/Python-3.x-3776AB?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Windows%20focused-0078D6)
![Tests](https://img.shields.io/badge/Tests-Manual-F39C12)
![Capture](https://img.shields.io/badge/Capture-Frame%20%2B%20Event-16A085)
![Status](https://img.shields.io/badge/README-Enhanced-2ECC71)

## نظرة عامة

`SyncImagingSystem` هو بيئة Python لالتقاط متزامن بين كاميرات الإطارات (Frame) وكاميرات الأحداث (Event).

يوفّر ثلاث مسارات عمل رئيسية نشطة:

1. `DualCamera_separate_transform_davis+evk.py`: واجهة موحّدة لالتقاط الإطار + الحدث (كاميرا إطار Hikrobot/Haikang + كاميرا أحداث EVK أو DAVIS).
2. `unified_event_gui.py`: واجهة أحداث فقط لأجهزة EVK و DAVIS.
3. `save_davis_tcp.py`: سكربت التقاط DAVIS يدعم نمط الكاميرا المباشر ونمط الشبكة عبر DV Viewer TCP.

يتضمن المستودع أيضًا حِزَم SDK/عينات من المورّدين ونماذج أولية تاريخية للرجوع إليها.

## الميزات

| المجال | أبرز النقاط |
|---|---|
| 🎛️ واجهة موحّدة | واجهة موحّدة لالتقاط الإطار + الحدث مع تحكم لكل جهاز وتحكم موحّد للبدء/الإيقاف. |
| ⚡ واجهة الأحداث | واجهة أحداث فقط بعمليات اتصال/معاينة/تسجيل متعددة الأجهزة. |
| 📡 مصادر DAVIS | التقاط DAVIS من العتاد مباشرة (`INPUT_MODE = "camera"`) أو من بث DV Viewer الشبكي (`INPUT_MODE = "network"`, المنافذ الافتراضية `7777/7778`). |
| 💾 صيغ الخرج | مخرجات التسجيل تشمل `.avi`, `.raw`, `.aedat4` وملف `events.npz` مضغوط اختياريًا. |
| 🗂️ تنظيم التشغيل | تنظيم تلقائي لمجلدات التشغيل المؤرخة زمنيًا تحت `recordings/` أو `davis_output/`. |
| 🔧 عناصر التحكم | تحكم بانحيازات EVK ضمن مسارات العمل ذات الواجهة الموحدة. |
| 🪞 تحويل الإطار | قلب عمودي، قلب أفقي، وتدوير 90 درجة في واجهة الكاميرتين. |
| 🖥️ النوافذ | أدوات مساعدة لتموضع نوافذ المعاينة ضمن سير عمل متعدد النوافذ (خصوصًا على Windows). |

## بنية المشروع

```text
SyncImagingSystem/
├── README.md
├── AGENTS.md
├── DualCamera_separate_transform_davis+evk.py   # Main unified frame+event GUI (EVK + DAVIS)
├── DualCamera_separate_transform.py             # Older integrated frame+EVK GUI variant
├── unified_event_gui.py                         # Event-only GUI for EVK + DAVIS
├── save_davis_tcp.py                            # DAVIS capture (camera or DV Viewer TCP)
├── code-legacy/                                 # Historical scripts/prototypes
├── evk_sdk/                                     # Prophesee/Metavision SDK scripts and samples
├── haikang_sdk/                                 # Hikrobot/Haikang SDK bundles and samples
├── i18n/                                        # Translation directory (currently empty)
├── recordings/                                  # Runtime output (gitignored, created on use)
└── davis_output/                                # Runtime output for save_davis_tcp.py (gitignored)
```

## المتطلبات المسبقة

### العتاد

- كاميرا إطار Hikrobot/Haikang (لعمليات الإطار).
- كاميرا أحداث EVK و/أو كاميرا أحداث DAVIS.

### نظام التشغيل

- Windows هو الهدف الأساسي لتكامل SDK الكامل لكاميرا الإطار ولسلوك تموضع نوافذ المعاينة.
- قد يعمل Linux/macOS مع أجزاء من مسار الأحداث، لكن التكافؤ الكامل غير مضمون.

### Python

- Python 3.x.

### حزم Python

ثبّت تبعيات التشغيل الأساسية في البيئة النشطة:

```bash
pip install numpy opencv-python dv-processing
```

لمسارات EVK، ثبّت حزم Python الخاصة بـ Prophesee Metavision المتاحة في بيئتك.

لسلوك التحكم بنوافذ Windows في معاينات الواجهة:

```bash
pip install pywin32
```

## التثبيت

1. انسخ (clone) المستودع.
2. افتح طرفية في جذر المستودع:

```bash
cd /home/lachlan/ProjectsLFS/SyncImagingSystem
```

3. أنشئ/فعّل بيئة Python الخاصة بك.
4. ثبّت التبعيات (انظر أعلاه).
5. تأكد من تثبيت runtimes/drivers الخاصة بـ SDK المطلوبة لأجهزتك.

ملاحظة افتراضية: مصفوفة إصدارات تعريفات/برمجيات المورد الدقيقة غير موثقة بالكامل داخل المستودع حتى الآن؛ حافظ على إعداد SDK المحلي المعروف لديك بأنه مستقر.

## الاستخدام

### 1) واجهة موحّدة للإطار + الحدث (مسار العمل المتكامل الموصى به)

```bash
python DualCamera_separate_transform_davis+evk.py
```

ما الذي توفره:

- فحص تلقائي لأجهزة الإطار والأحداث عند بدء التشغيل.
- عناصر تحكم كاميرا الإطار: اتصال، التقاط، معاينة، تسجيل، تعريض/كسب.
- عناصر تحكم كاميرا الأحداث: اتصال، التقاط، عرض مرئي، تسجيل.
- عناصر تحكم موحدة: بدء/إيقاف المعاينة والتسجيل للطرفين معًا.
- تحكم بمجلد الخرج وبادئة اسم الملف ضمن الواجهة.

سلوك الخرج الافتراضي:

| الخرج | النمط |
|---|---|
| المجلد الأساسي | `recordings/` |
| مجلد التشغيل | `<prefix>_<timestamp>/` |
| ملفات الإطار | `<frame_device_label>/<prefix>_frame_<timestamp>.avi` |
| ملفات الأحداث (EVK) | `<event_device_label>/<prefix>_<timestamp>.raw` |
| ملفات الأحداث (DAVIS) | `<event_device_label>/output.aedat4` (+ `events.npz` عند الإيقاف) |

### 2) واجهة أحداث فقط

```bash
python unified_event_gui.py
```

السلوك الافتراضي:

- المجلد الأساسي للخرج: `recordings/`
- بادئة التشغيل الافتراضية: `session`
- اكتشاف الأجهزة:
  - DAVIS عبر `dv.io.camera.discover()`
  - EVK كـ `EVK:auto` عندما تكون وحدات Metavision متاحة
- مخرجات التسجيل:
  - EVK: `.raw`
  - DAVIS: `output.aedat4` و `events.npz` (إذا وُجدت أحداث مخزنة مؤقتًا)

### 3) سكربت التقاط DAVIS (كاميرا أو DV Viewer TCP)

```bash
python save_davis_tcp.py
```

الثوابت الأساسية الافتراضية في السكربت:

| الثابت | الافتراضي |
|---|---|
| `INPUT_MODE` | `"camera"` (`"network"` لـ DV Viewer TCP) |
| `HOST` | `"127.0.0.1"` |
| `EVENTS_PORT` | `7777` |
| `FRAMES_PORT` | `7778` |
| `CAPTURE_SECONDS` | `3.0` |
| `SAVE_EVENTS_NPZ` | `True` |
| `SAVE_FRAMES_VIDEO` | `True` |
| `SAVE_AEDAT4` | `True` |
| `SHOW_EVENT_PREVIEW` | `True` |

صيغة مجلد الخرج:

- `davis_output/<YYYYmmdd_HHMMSS>/`
- ملفات نموذجية: `events.npz`, `frames.avi`, `output.aedat4`

## الإعداد

### `save_davis_tcp.py`

عدّل الثوابت العلوية المكتوبة بأحرف كبيرة لضبط:

- مصدر الإدخال (`INPUT_MODE`)
- نقطة نهاية الشبكة (`HOST`, `EVENTS_PORT`, `FRAMES_PORT`)
- مدة الالتقاط (`CAPTURE_SECONDS`)
- مفاتيح تفعيل الخرج (`SAVE_EVENTS_NPZ`, `SAVE_FRAMES_VIDEO`, `SAVE_AEDAT4`)
- سلوك المعاينة (`SHOW_EVENT_PREVIEW`, `PREVIEW_FPS`, `PREVIEW_WINDOW_NAME`)

### `DualCamera_separate_transform_davis+evk.py`

إعدادات وقت التشغيل المعروضة في الواجهة تشمل:

- مجلد الخرج وبادئة اسم الملف
- تحويلات الإطار (قلب عمودي/أفقي، تدوير)
- تحكم التعريض والكسب للإطار
- تحكم انحيازات EVK (`bias_diff`, `bias_diff_off`, `bias_diff_on`, `bias_fo`, `bias_hpf`, `bias_refr`) عند الدعم

### `unified_event_gui.py`

القيم الافتراضية الأساسية (قابلة للتعديل في السكربت):

- `DEFAULT_OUTPUT_DIR = "recordings"`
- `DEFAULT_PREFIX = "session"`
- `PREVIEW_FPS = 30.0`

## أمثلة

### مثال A: التقاط DAVIS مباشر لمدة 10 ثوانٍ

عدّل `save_davis_tcp.py`:

```python
INPUT_MODE = "camera"
CAPTURE_SECONDS = 10.0
SAVE_AEDAT4 = True
SAVE_EVENTS_NPZ = True
SAVE_FRAMES_VIDEO = True
```

شغّل:

```bash
python save_davis_tcp.py
```

### مثال B: استقبال بيانات DAVIS من DV Viewer عبر TCP

عدّل `save_davis_tcp.py`:

```python
INPUT_MODE = "network"
HOST = "127.0.0.1"
EVENTS_PORT = 7777
FRAMES_PORT = 7778
```

شغّل:

```bash
python save_davis_tcp.py
```

### مثال C: جلسة أحداث فقط مع توصيل كلٍ من EVK و DAVIS

```bash
python unified_event_gui.py
```

ثم داخل الواجهة:

1. انقر `Scan`.
2. وصّل الأجهزة المحددة.
3. اضبط مجلد الخرج/البادئة.
4. استخدم `Record All` لبدء مجلدات خرج متزامنة لكل تشغيل.

## ملاحظات التطوير

- لا يوجد حاليًا نظام بناء أو بيانات تعريف حزمة (`pyproject.toml`, `requirements.txt`, إلخ غير موجودة).
- يتم تشغيل السكربتات مباشرة عبر نقاط دخول Python.
- الإعداد يعتمد غالبًا على ثوابت السكربت وعناصر تحكم الواجهة، وليس أعلام CLI.
- أدلة SDK الخاصة بالمورّدين محفوظة عمدًا داخل المستودع:
  - `evk_sdk/`
  - `haikang_sdk/`
- نواتج/بيانات التشغيل مُدرجة في `.gitignore`، بما يشمل:
  - `recordings/`, `davis_output/`, `data/`, `*.aedat4`, `*.raw`, `*.avi`, `*.npz`, إلخ.
- واجهة الكاميرتين تتضمن منطق تموضع للمعاينات مصممًا لتقليل الظهور المفاجئ للنوافذ ومنع حجب عناصر التحكم الرئيسية، خصوصًا على Windows.

## استكشاف الأخطاء وإصلاحها

| العَرَض | الفحوصات / الإجراءات |
|---|---|
| أخطاء استيراد `dv_processing` | ثبّت أو أصلح `dv-processing` في البيئة النشطة. نمط كاميرا DAVIS المباشر في `save_davis_tcp.py` يتطلب `dv-processing`. |
| أخطاء استيراد/وحدات EVK (`metavision_*`) | تأكد من تثبيت Metavision SDK ووحدات Python الخاصة به وإتاحتها ضمن مسار Python. |
| فشل استيراد SDK كاميرا الإطار (`MvCameraControl_class`, إلخ) | تحقّق من وجود ملفات Hikrobot/Haikang SDK واعتماديات وقت التشغيل. تأكد أن مسارات SDK المحلية المستخدمة في السكربتات صحيحة. |
| لم يتم العثور على أجهزة | افحص توصيل الكاميرا والطاقة والصلاحيات. أعد تنفيذ `Scan` من الواجهة بعد إعادة توصيل العتاد. |
| معاينة DAVIS لا تعرض الأحداث فورًا | قد تُفتح نافذة المعاينة بإطار فارغ حتى تصل حزم الأحداث. |
| المعاينة ليست always-on-top دائمًا أو التموضع ليس كما هو متوقع | على Windows ثبّت `pywin32`؛ على المنصات غير Windows يكون السلوك محدودًا. |
| ملفات التسجيل تفتقد محتوى متوقعًا | بعض الملفات تُستكمل عند الإيقاف؛ تأكد من إيقاف التسجيل بشكل صحيح قبل إغلاق التطبيق. |

## خارطة الطريق

- إضافة ملفات تبعيات مثبتة الإصدارات (`requirements.txt` أو `pyproject.toml`).
- إضافة اختبارات آلية مستقلة عن العتاد لمنطق الأدوات المساعدة.
- توسيع التوثيق لتركيبات العتاد/التعريف/الإصدار التي تم التحقق منها.
- إضافة معاملات CLI للثوابت المرمزة حاليًا داخل السكربتات.
- إضافة ملفات README متعددة اللغات في `i18n/` وربطها من سطر خيارات اللغة.

## المساهمة

المساهمات مرحّب بها.

سير عمل مقترح:

1. أنشئ فرعًا لتغييراتك.
2. حافظ على التعديلات مركّزة وآمنة على العتاد.
3. تحقّق بتشغيل السكربتات ذات الصلة على الأجهزة المتاحة.
4. تجنّب رفع تسجيلات/بيانات مولدة كبيرة.
5. افتح PR يتضمن:
   - بيئة العتاد/البرمجيات
   - إعداد الكاميرا
   - إعدادات المنافذ/العارض (لسير العمل الشبكي)
   - مسارات/سجلات خرج نموذجية

ملاحظة حول اصطلاحات المستودع: رسائل commit الحالية خفيفة؛ استخدم رسائل أمرية قصيرة (على سبيل المثال: `Add DAVIS capture docs`).

## الترخيص

لا يوجد حاليًا ملف ترخيص صريح في هذا المستودع.

ملاحظة افتراضية: إذا كان المشروع مخصصًا لإعادة التوزيع، أضف ملف `LICENSE` وحدّث هذا القسم.

## الشكر والتقدير

- منظومة Prophesee Metavision (`evk_sdk/` ووحدات Python ذات الصلة).
- منظومة iniVation/dv-processing للتعامل مع DAVIS.
- موارد SDK الخاصة بكاميرات Hikrobot/Haikang المضمنة تحت `haikang_sdk/`.
