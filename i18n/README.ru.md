[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# SyncImagingSystem

Дополнительные локализованные README находятся в `i18n/`.

![Python](https://img.shields.io/badge/Python-3.x-3776AB?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Windows%20focused-0078D6)
![Tests](https://img.shields.io/badge/Tests-Manual-F39C12)
![Capture](https://img.shields.io/badge/Capture-Frame%20%2B%20Event-16A085)
![Status](https://img.shields.io/badge/README-Enhanced-2ECC71)

## Обзор

`SyncImagingSystem` — это Python-рабочее пространство для синхронизированного захвата кадровых и событийных камер.

Оно предоставляет три основных активных сценария:

1. `DualCamera_separate_transform_davis+evk.py`: единый GUI для захвата кадров + событий (кадровая камера Hikrobot/Haikang + событийная камера EVK или DAVIS).
2. `unified_event_gui.py`: GUI только для событийных устройств EVK и DAVIS.
3. `save_davis_tcp.py`: скрипт захвата DAVIS с поддержкой прямого режима камеры и сетевого режима DV Viewer TCP.

Репозиторий также содержит наборы вендорских SDK/примеров и исторические прототипы для справки.

## Возможности

| Область | Ключевые возможности |
|---|---|
| 🎛️ Единый GUI | Единый GUI захвата кадров + событий с управлением по устройствам и общими кнопками запуска/остановки. |
| ⚡ Event GUI | GUI только для событийных устройств с операциями подключения/предпросмотра/записи для нескольких устройств. |
| 📡 Источники DAVIS | Захват DAVIS с прямого устройства (`INPUT_MODE = "camera"`) или сетевого потока DV Viewer (`INPUT_MODE = "network"`, порты по умолчанию `7777/7778`). |
| 💾 Форматы вывода | Результаты записи включают `.avi`, `.raw`, `.aedat4` и опционально сжатый `events.npz`. |
| 🗂️ Организация запусков | Автоматическая организация по папкам запуска с меткой времени в `recordings/` или `davis_output/`. |
| 🔧 Управление | Управление bias EVK в сценариях с единым GUI. |
| 🪞 Трансформации кадра | Вертикальное отражение, горизонтальное отражение и поворот на 90 градусов в GUI двух камер. |
| 🖥️ Окна | Помощники размещения окон предпросмотра для многоконных сценариев (особенно на Windows). |

## Структура проекта

```text
SyncImagingSystem/
├── README.md
├── AGENTS.md
├── DualCamera_separate_transform_davis+evk.py   # Main unified frame+event GUI (EVK + DAVIS)
├── DualCamera_separate_transform.py             # Older integrated frame+EVK GUI variant
├── unified_event_gui.py                         # Event-only GUI for EVK + DAVIS
├── save_davis_tcp.py                            # DAVIS capture (camera or DV Viewer TCP)
├── code-legacy/                                 # Historical scripts/prototypes
├── evk_sdk/                                     # Prophesee/Metavision SDK scripts and samples
├── haikang_sdk/                                 # Hikrobot/Haikang SDK bundles and samples
├── i18n/                                        # Translation directory (currently empty)
├── recordings/                                  # Runtime output (gitignored, created on use)
└── davis_output/                                # Runtime output for save_davis_tcp.py (gitignored)
```

## Требования

### Оборудование

- Кадровая камера Hikrobot/Haikang (для сценариев с кадровым потоком).
- Событийная камера EVK и/или событийная камера DAVIS.

### ОС

- Windows — основная целевая платформа для полной интеграции SDK кадровой камеры и поведения размещения окон предпросмотра.
- Linux/macOS могут запускать части событийного конвейера, но полное соответствие не гарантируется.

### Python

- Python 3.x.

### Python-пакеты

Установите основные runtime-зависимости в активное окружение:

```bash
pip install numpy opencv-python dv-processing
```

Для сценариев EVK установите Python-пакеты Prophesee Metavision, доступные в вашем окружении.

Для поведения управления окнами в предпросмотре GUI на Windows:

```bash
pip install pywin32
```

## Установка

1. Клонируйте репозиторий.
2. Откройте терминал в корне репозитория:

```bash
cd /home/lachlan/ProjectsLFS/SyncImagingSystem
```

3. Создайте/активируйте ваше Python-окружение.
4. Установите зависимости (см. выше).
5. Убедитесь, что для ваших устройств установлены необходимые runtime-компоненты/драйверы SDK камер.

Примечание-предположение: точная матрица версий драйверов/прошивок вендоров пока не полностью задокументирована в репозитории; сохраните вашу локальную рабочую конфигурацию SDK.

## Использование

### 1) Единый GUI кадров + событий (рекомендуемый интегрированный сценарий)

```bash
python DualCamera_separate_transform_davis+evk.py
```

Что предоставляет:

- Автосканирование кадровых и событийных устройств при запуске.
- Управление кадровой камерой: подключение, захват, предпросмотр, запись, экспозиция/усиление.
- Управление событийной камерой: подключение, захват, визуализация, запись.
- Общие элементы управления: запуск/остановка предпросмотра и записи для обеих сторон одновременно.
- Управление выходной директорией + префиксом имени файла в GUI.

Поведение вывода по умолчанию:

| Output | Pattern |
|---|---|
| Base directory | `recordings/` |
| Run folder | `<prefix>_<timestamp>/` |
| Frame files | `<frame_device_label>/<prefix>_frame_<timestamp>.avi` |
| Event files (EVK) | `<event_device_label>/<prefix>_<timestamp>.raw` |
| Event files (DAVIS) | `<event_device_label>/output.aedat4` (+ `events.npz` on stop) |

### 2) GUI только для событийных устройств

```bash
python unified_event_gui.py
```

Поведение по умолчанию:

- Базовая директория вывода: `recordings/`
- Префикс запуска по умолчанию: `session`
- Обнаружение устройств:
  - DAVIS из `dv.io.camera.discover()`
  - EVK как `EVK:auto`, когда доступны модули Metavision
- Выходные файлы записи:
  - EVK: `.raw`
  - DAVIS: `output.aedat4` и `events.npz` (если есть буферизованные события)

### 3) Скрипт захвата DAVIS (камера или DV Viewer TCP)

```bash
python save_davis_tcp.py
```

Ключевые константы по умолчанию в скрипте:

| Constant | Default |
|---|---|
| `INPUT_MODE` | `"camera"` (`"network"` for DV Viewer TCP) |
| `HOST` | `"127.0.0.1"` |
| `EVENTS_PORT` | `7777` |
| `FRAMES_PORT` | `7778` |
| `CAPTURE_SECONDS` | `3.0` |
| `SAVE_EVENTS_NPZ` | `True` |
| `SAVE_FRAMES_VIDEO` | `True` |
| `SAVE_AEDAT4` | `True` |
| `SHOW_EVENT_PREVIEW` | `True` |

Формат выходной директории:

- `davis_output/<YYYYmmdd_HHMMSS>/`
- Типичные файлы: `events.npz`, `frames.avi`, `output.aedat4`

## Конфигурация

### `save_davis_tcp.py`

Настраивайте верхнеуровневые константы в верхнем регистре для управления:

- источником входа (`INPUT_MODE`)
- сетевой конечной точкой (`HOST`, `EVENTS_PORT`, `FRAMES_PORT`)
- длительностью захвата (`CAPTURE_SECONDS`)
- флагами вывода (`SAVE_EVENTS_NPZ`, `SAVE_FRAMES_VIDEO`, `SAVE_AEDAT4`)
- поведением предпросмотра (`SHOW_EVENT_PREVIEW`, `PREVIEW_FPS`, `PREVIEW_WINDOW_NAME`)

### `DualCamera_separate_transform_davis+evk.py`

Настройки времени выполнения, доступные в GUI, включают:

- папку вывода и префикс имени файла
- трансформации кадра (вертикальное/горизонтальное отражение, поворот)
- управление экспозицией и усилением кадра
- управление bias EVK (`bias_diff`, `bias_diff_off`, `bias_diff_on`, `bias_fo`, `bias_hpf`, `bias_refr`) при поддержке

### `unified_event_gui.py`

Ключевые значения по умолчанию (редактируются в скрипте):

- `DEFAULT_OUTPUT_DIR = "recordings"`
- `DEFAULT_PREFIX = "session"`
- `PREVIEW_FPS = 30.0`

## Примеры

### Пример A: Прямой захват DAVIS-камеры на 10 секунд

Отредактируйте `save_davis_tcp.py`:

```python
INPUT_MODE = "camera"
CAPTURE_SECONDS = 10.0
SAVE_AEDAT4 = True
SAVE_EVENTS_NPZ = True
SAVE_FRAMES_VIDEO = True
```

Запуск:

```bash
python save_davis_tcp.py
```

### Пример B: Получение данных DAVIS из DV Viewer по TCP

Отредактируйте `save_davis_tcp.py`:

```python
INPUT_MODE = "network"
HOST = "127.0.0.1"
EVENTS_PORT = 7777
FRAMES_PORT = 7778
```

Запуск:

```bash
python save_davis_tcp.py
```

### Пример C: Сессия только событий с подключенными EVK и DAVIS

```bash
python unified_event_gui.py
```

Затем в GUI:

1. Нажмите `Scan`.
2. Подключите выбранные устройства.
3. Укажите папку вывода/префикс.
4. Используйте `Record All` для запуска синхронизированных выходных папок на каждый запуск.

## Заметки для разработки

- В данный момент не определены система сборки или метаданные пакета (`pyproject.toml`, `requirements.txt` и т. д. отсутствуют).
- Скрипты запускаются напрямую через Python entrypoints.
- Конфигурация в основном задается константами скриптов и элементами GUI, а не CLI-флагами.
- Директории вендорских SDK намеренно хранятся в репозитории:
  - `evk_sdk/`
  - `haikang_sdk/`
- Артефакты вывода/данных добавлены в gitignore, включая:
  - `recordings/`, `davis_output/`, `data/`, `*.aedat4`, `*.raw`, `*.avi`, `*.npz`, и т. д.
- GUI двух камер включает логику размещения предпросмотров, рассчитанную на уменьшение pop-in окон и предотвращение перекрытия основных элементов управления, особенно на Windows.

## Устранение неполадок

| Симптом | Проверки / Действия |
|---|---|
| Ошибки импорта `dv_processing` | Установите или восстановите `dv-processing` в активном окружении. Прямой режим DAVIS-камеры в `save_davis_tcp.py` требует `dv-processing`. |
| Ошибки импорта/модулей EVK (`metavision_*`) | Убедитесь, что SDK Metavision/Python-модули установлены и доступны в вашем Python path. |
| Сбои импорта SDK кадровой камеры (`MvCameraControl_class` и т. п.) | Проверьте наличие файлов SDK Hikrobot/Haikang и runtime-зависимостей. Убедитесь, что локальные пути SDK, используемые скриптами, корректны. |
| Устройства не найдены | Проверьте подключение камеры, питание и права доступа. После переподключения оборудования снова запустите `Scan` в GUI. |
| Предпросмотр DAVIS не показывает события сразу | Окно предпросмотра может открыться с пустым кадром до поступления пакетов событий. |
| Предпросмотр не всегда поверх остальных окон или расположен не как ожидается | На Windows установите `pywin32`; на платформах не-Windows поведение ограничено. |
| В файлах записи отсутствует ожидаемое содержимое | Некоторые файлы финализируются при остановке; перед закрытием приложения убедитесь, что запись корректно остановлена. |

## Дорожная карта

- Добавить зафиксированные файлы зависимостей (`requirements.txt` или `pyproject.toml`).
- Добавить аппаратно-независимые автоматизированные тесты для вспомогательной логики.
- Расширить документацию по проверенным комбинациям оборудования/драйверов/версий.
- Добавить CLI-аргументы для констант скриптов, которые сейчас захардкожены.
- Добавить многоязычные README-файлы в `i18n/` и связать их через строку language-options.

## Вклад

Вклад приветствуется.

Рекомендуемый рабочий процесс:

1. Создайте ветку для вашего изменения.
2. Держите изменения сфокусированными и безопасными для работы с оборудованием.
3. Проверяйте изменения запуском соответствующих скриптов на доступных устройствах.
4. Не коммитьте крупные сгенерированные записи/данные.
5. Откройте PR с описанием:
   - аппаратной/программной среды
   - конфигурации камер
   - настроек портов/viewer (для сетевых сценариев)
   - примеров путей вывода/логов

Примечание по соглашениям репозитория: сообщения коммитов пока в облегчённом стиле; используйте короткие формулировки в повелительном наклонении (например: `Add DAVIS capture docs`).

## Лицензия

В этом репозитории сейчас нет явного файла лицензии.

Примечание-предположение: если проект предназначен для распространения, добавьте файл `LICENSE` и обновите этот раздел.

## Благодарности

- Экосистема Prophesee Metavision (`evk_sdk/` и связанные Python-модули).
- Экосистема iniVation/dv-processing для работы с DAVIS.
- Ресурсы SDK камер Hikrobot/Haikang, включённые в `haikang_sdk/`.
